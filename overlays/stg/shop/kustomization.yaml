# stg shop-unit overlay (Plan 03-09, Task 1) — mirrors
# overlays/dev/shop: same chart templates, stg's namespace; rendered
# product is ../../../envs/stg/athena-shop/all.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stg
resources:
  - rendered-input.yaml
